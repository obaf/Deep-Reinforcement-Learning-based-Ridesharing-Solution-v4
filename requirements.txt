# trip_simulation4 — runtime dependencies (direct only, exact pins)
#
# Pinned at first successful install on Python 3.14.3 / Windows.
# Full transitive snapshot lives in `requirements.lock.txt`.
#
# Notes:
#  * `pyogrio` replaces `fiona` because Python 3.14 has no fiona wheel
#    and fiona's source build needs a system GDAL toolchain. Geopandas
#    1.x uses pyogrio as its default engine, so behaviour is identical
#    for our needs (read the taxi_zones.shp file).
#  * The plan's § 12 suggested ceilings (`pandas<2.4`, `numpy<2.2`,
#    `torch<2.6`) had no Python 3.14 wheels at install time and have
#    been bumped accordingly.

pandas==3.0.2
pyarrow==24.0.0
numpy==2.4.4
scipy==1.17.1
torch==2.11.0
gymnasium==1.3.0
geopandas==1.1.3
shapely==2.1.2
pyogrio==0.12.1
python-docx==1.2.0
lxml==6.1.0
matplotlib==3.10.9
openpyxl==3.1.5
tqdm==4.67.3
requests==2.33.1
